# v1.1 Ship List

Single source of truth for remaining v1.1 work. Everything here must be resolved before shipping.

---

## Remaining Features

### Import Report
- [x] Define structured import report schema (summary, warnings, conflicts, per-row outcomes).
- [x] Implement report generation after import runs.
- [x] Surface report in import sheet as a post-import summary view before dismissal.

---

## Import Hardening

- [x] Preview/stage parity: row-order fallback preview labels should show "Row 001", "Row 002", etc.
- [x] Preview/stage parity: surface fallback warning clearly in the import sheet (not hidden in popover).
- [x] EOS field dependency: clarify expected behaviour for Lens Model greyed out when Focal Length deselected (E10). Update test expectation or UI.
- [x] Add regression tests covering preview/stage parity and import report content.

---

## UX Polish

- [x] Export scope explicitness (X1): make export intent explicit — "Folder (N files)" vs "Selection (N files)" — in sheet copy and confirmation messaging.
- [x] CSV warning quality (X4): reduce warning noise in preview for ExifTool-origin files; prioritise concise grouped summary before per-row detail.
- [ ] EOS lens deconfliction dialog layout (E6): polish spacing, hierarchy, and readability.
- [x] Preview text legibility: improve scannability; evaluate surfacing key warnings in the main sheet rather than only in the preview popover.
- [ ] Status surface rationalisation: remove bottom-of-inspector status box; define one clear rule for where state information appears.
- [x] Sort order consistency: ensure the four sort options appear in the same order across toolbar menu, View menu, and any other surfaces (QA_MATRIX note).

---

## Bug Fixes (from code review, this session)

All fixed and committed. Listed here for QA verification:

- [x] `isBusy` not reset on import preview task cancellation.
- [x] `preparedRun` not cleared in `performImport` error path.
- [x] Empty import (zero staged files) now shows error instead of silent no-op.
- [x] Inspector settings: targeted checkbox updates instead of full view rebuild.
- [x] `PresetEditorSheet` state leakage (`validationMessage`, `duplicateConflict`) on reopen.
- [x] `PresetManagerSheet` `pendingDeletePresetID` not reset on reopen.
- [x] Photos staging directory leaked on copy failure — tracked immediately after creation.
- [x] Photos staging directory not cleaned on `NSWorkspace.open` error.
- [x] `menuTrackingObserver` and `appearanceObservation` not cleaned in teardown.
- [x] EOS lens `remainingAmbiguousRowsByFocal` default value mismatch (0 vs 1).
- [x] Toolbar buttons not updating on macOS light/dark mode switch (KVO on `effectiveAppearance`).
- [x] Apply confirmation: consolidated into single sheet; removed blocking `runModal` from AppModel.
- [x] Settings General pane: auto-sized via `fittingSize` instead of hardcoded height.
- [x] Settings Inspector pane: scroll view starts at top (FlippedView).
- [x] Settings window: clamped to visible screen height.
- [x] All `file(s)` / `image(s)` / `backup(s)` pluralisation replaced with proper ternary (16 instances).
- [x] Inspector preview rotation regression: after Apply, preview could show stale pre-apply orientation while thumbnail was correct. Fixed by immediate thumbnail-cache invalidation + forced selected-preview reload on apply/restore success.

---

## Architecture / Code Health (completed this session)

- [x] Extracted sidebar and preview flows from `AppModel.swift` into dedicated extensions (`7b1a69e`).
- [x] Split `AppModel.swift` by concern into focused extensions (actions, apply/restore, editing, file loading, metadata pipeline/values, import, navigation, presets, field catalog, undo status) (`ea54e15`).
- [x] Tightened post-split access control and helper visibility to reduce accidental cross-file coupling (`482f9c6`).

## Stability / Performance Fixes (completed this session)

- [x] Optimized thumbnail cache behavior and hardened UI task lifecycle/cleanup paths (`ceafcd3`).
- [x] Improved UI observer reuse and background warm scheduling behavior (`3598163`).
- [x] Consolidated apply confirmation flow and fixed settings sizing/toolbar appearance regressions (`82f558f`).

---

## Manual QA

- [ ] Re-run `import-manual-smoke-checklist.md` end-to-end (CSV, ExifTool CSV, EOS 1V, GPX, Reference Folder).
- [ ] Final v1.1 import-sheet review-mode smoke: clean import auto-closes; warning/conflict/partial runs stay in-sheet with single banner precedence, `Import` -> `Close`, `Fields`/`Advanced` disabled, and `Details…` showing structured report content.
- [ ] Complete global checks: Fields filter, If-no-match policy, no freezes, no crashes, status message counts.
- [ ] QA matrix: verify restore partial-failure subtitle ("Restored X of N — Y failed").
- [ ] QA matrix: full subtitle state table pass.
- [ ] Settings: verify `Cmd+,` and app menu Settings both open the settings window reliably.
- [ ] Verify all bug fixes listed above in a live build.

---

## Release Prep

- [ ] Reconcile version/baseline inconsistencies across Roadmap.md and CHANGELOG.md.
- [ ] Finalise CHANGELOG.md v1.1 section.
- [ ] Write release notes.
- [ ] Final build + test on release candidate.
- [ ] Archive, notarise, DMG, verify install-launch sanity.

---

## Out of Scope (deferred to later releases)

The following items appeared in v1.1 ticket lists but belong to future releases per Roadmap.md:

- Rubber-band drag selection in list/gallery views → **v1.3**
- QuickLook panel size inconsistency (GitHub issue #9) → **v1.3** (full QuickLook rewrite)
- Backup retention policy (keep-last-N model, prune wiring, Settings UI) → **v2.0**
