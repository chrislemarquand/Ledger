# v1.1 Ticket List

1. Settings: polish window layout and tab sizing behavior.
2. Settings: implement backup retention policy model (`keep last N`), defaults, and persistence.
3. Settings: wire retention policy to backup prune path and add unit tests.
4. Settings: add retention UI controls in General tab with validation.
5. Settings: manual QA pass for `Settings…` app menu and `Cmd+,` reliability.
6. Import: define structured import report schema (summary, warnings, conflicts, per-row outcomes).
7. Import: implement report generation after preview/import runs.
8. Import: add report export/save action and status messaging.
9. Import: harden preview/stage parity for row-order fallback labels/warnings.
10. Import: add regression tests covering parity + report content.
11. Import: re-run and complete `import-manual-smoke-checklist.md`, including global checks and sign-off.
12. Browser: implement rubber-band drag selection in list view (with modifier semantics).
13. Browser: implement rubber-band drag selection in gallery view (with autoscroll).
14. Browser: add selection regression tests (plain/cmd/shift drag + cross-mode consistency).
15. Photos handoff: add command(s) and scope resolution (`Selection` vs `Folder`).
16. Photos handoff: implement execution path + failure messaging/tests.
17. Lightroom handoff: add command(s) and scope resolution.
18. Lightroom handoff: implement execution path + failure messaging/tests.
19. QA: complete outstanding QA matrix checks (restore partial-failure subtitle + full subtitle table).
20. Docs: reconcile version/baseline inconsistencies across roadmap/changelog.
21. Release prep: finalize changelog `v1.1` section and release notes.
22. Release gate: run final build/test/release checks on RC.
23. Release: run archive/notarize/DMG flow and verify install-launch sanity on artifact.
