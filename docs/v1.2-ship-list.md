# v1.2 Ship List

Single source of truth for remaining v1.2 work. Everything here must be resolved or explicitly deferred before shipping.

---

## Scope Decision

### Recommended v1.2 release shape

- [ ] Lock v1.2 scope as: **Batch Rename + editing-productivity polish only**.
- [ ] Move the remaining unimplemented `Roadmap.md` items out of v1.2 unless they are actually built and tested before code freeze.

### If scope is not narrowed

The following remain release blockers because they are still listed under v1.2 in `Roadmap.md`:

- [ ] Additional Inspector EXIF fields, including rating.
- [ ] Timestamp sync tools:
  - [ ] `Set file date from DateTimeOriginal`.
  - [ ] `Set DateTimeOriginal from file date`.
- [ ] ExifTool-native `DateTimeDigitized` backfill.
- [ ] Metadata copy/paste:
  - [ ] Field-level copy/paste.
  - [ ] Metadata-set copy/paste.

---

## Remaining Features

### Batch Rename Release Gate

- [ ] Confirm the first-release token set is final.
- [ ] Confirm Batch Rename sheet copy, labels, and defaults are final.
- [ ] Confirm rename failure messaging is acceptable in normal use.
- [ ] Confirm folder-scope rename behaviour is safe and understandable.
- [ ] Confirm backup/restore behaviour is acceptable for success, partial failure, and restore flows.

### Implemented on branch and in scope for v1.2

These are already materially present on `codex/v1.2` and should be treated as the candidate v1.2 headline work:

- [x] Core token-based rename engine in `ExifEditCore`.
- [x] Selection-scope and folder-scope rename actions.
- [x] Preview plan generation.
- [x] Collision disambiguation.
- [x] Backup-aware execution and restore support for renamed outputs.
- [x] Deterministic ordering in preview/build plan.
- [x] Batch Rename sheet UI.
- [x] Core service tests.
- [x] App-model action-state tests.

---

## Hardening

- [ ] Verify rename preview parity with actual execution ordering and filenames.
- [ ] Verify rename collisions resolve deterministically across repeated runs.
- [ ] Verify restore works after rename batches that include mixed extensions and sequence tokens.
- [ ] Verify browser, inspector, QuickLook, and selection state stay coherent after rename completes.
- [ ] Verify no stale file references remain after rename or restore.

---

## UX Polish

- [ ] Verify menu placement and naming are final across Image menu, toolbar/commands, and selection vs folder flows.
- [ ] Verify Batch Rename sheet presentation works correctly from both list and gallery workflows.
- [ ] Verify rename completion status messaging is concise and clear.
- [ ] Decide whether the Lightroom handoff additions are part of the v1.2 release notes or held for a later commit/release.

Already landed on branch and should be QA-verified:

- [x] Visual consistency pass across import, batch rename, and preset sheets.
- [x] Send to Photos / Lightroom Classic context menus in gallery and list views.
- [x] Send to Lightroom handoff and resolver hardening for Adobe Lightroom.

---

## Manual QA

### Batch Rename smoke

- [ ] Rename a selection with simple text tokens.
- [ ] Rename a selection with a sequence token.
- [ ] Rename a whole folder with a sequence token.
- [ ] Rename with extension override.
- [ ] Rename with existing name collisions present.
- [ ] Rename and then restore from backup.
- [ ] Verify browser, inspector, selection, and QuickLook remain coherent after rename.

### General release-candidate smoke

Run the hybrid release smoke from `ARCHITECTURE.md` on the release candidate:

- [ ] Launch app to default window.
- [ ] Click sidebar folder/source.
- [ ] Click between thumbnails/list rows rapidly; selection stays stable.
- [ ] Switch folders; browser repopulates correctly.
- [ ] Toggle inspector/sidebar and resize panes; no re-entrant warning.
- [ ] Edit metadata field and Apply; browser + inspector refresh coherently.
- [ ] Repeat after switching gallery <-> list.

Must-fail conditions:

- [ ] No `Publishing changes from within view updates is not allowed`.
- [ ] No `NSHostingView is being laid out reentrantly while rendering its SwiftUI content`.

---

## Release Prep

- [ ] Reconcile `Roadmap.md`, `CHANGELOG.md`, and release version/build metadata.
- [ ] Finalise the v1.2 changelog section.
- [ ] Write release notes.
- [ ] `./scripts/test/run_all.sh` passes on the release candidate commit.
- [ ] Final build + test on the release candidate.
- [ ] Archive, notarise, create DMG, and verify install/launch sanity using `docs/RELEASE.md`.

---

## Out of Scope (if v1.2 is narrowed)

The following should be explicitly deferred to a later release if v1.2 ships as a Batch Rename-focused release:

- [ ] Additional Inspector EXIF fields, including rating.
- [ ] Timestamp sync tools.
- [ ] ExifTool-native `DateTimeDigitized` backfill.
- [ ] Metadata copy/paste.
