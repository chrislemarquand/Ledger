# Ledger v1.2 Smoke Status

Conversation handoff snapshot as of 2026-04-03.

Current Ledger RC branch state:
- Branch: `codex/v1.2`
- Latest local verification run:
  - `swift test` passed on `codex/v1.2`
  - Result: `182` tests executed, `1` skipped, `0` failed

## Passed In This Conversation

### Welcome / Launch

- Welcome / What's New appears after reset.
- Welcome / What's New dismisses cleanly with no system beep.
- Welcome implementation was moved to a real SwiftUI `.sheet(...)` path.

### Sidebar / Navigation / Shell

- Sidebar favourites / recents interactions passed.
- Path bar visibility and persistence passed.
- Short stability pass passed:
  - list/gallery switching
  - pane resize
  - sidebar / inspector toggling

### List View

- `Esc` clears selection correctly and stays cleared.
- `Return` now focuses the inspector.
- Thumbnail flicker when moving selection in list view is fixed.
- Quick-click selection snap-back on initial folder load is fixed.
- Final quick list pass passed:
  - no thumbnail flicker
  - no selection snap-back

### Gallery View

- `Return` now focuses the inspector.
- Selected staged-rename label styling now shows an orange pill with white text.

### Inspector / Metadata

- Editorial fields now persist after Apply.
- Rating / flag / colour label are working again after the Welcome overlay regression was removed.
- Date/Time adjust sheet implemented and validated in-app.
- Set Location sheet implemented and validated in-app.
- Inspector date/time controls and Set buttons were aligned with current workflow.

### Apply / Restore / Backups

- Mixed metadata + staged rename apply / restore passed.
- Restore actions correctly disable when backups are off.

### Batch Rename

- Representative batch rename flow passed:
  - stage rename
  - preview looks sane
  - apply works
  - restore works
- Earlier broader batch rename pass also passed functionally.
- Batch rename UI overhauled in 2026-04-03 session:
  - Token row layout replaced with fixed 3-column grid (152 / 142 / 142 pt) using `InspectorPopupField` for all pickers.
  - `Current Name` token added with component (Name, Extension, Name + Extension, Number Suffix) and casing (Original, UPPERCASE, lowercase) pickers.
  - All token type labels, user-facing strings and validation messages updated to HIG style.
  - Inline preview (current name → new name) added below token rows.
  - `UPPERCASE` / `lowercase` picker width issue resolved by switch to `InspectorPopupField`.

### Presets

- Presets menu consolidated to single shared factory (no drift between image menu and toolbar).
- Image menu restructured: `Apply Preset ▶` submenu + `Save Metadata as Preset…` + `Manage Presets…` as direct siblings (no wrapper submenu).
- Toolbar keeps existing flat dropdown.
- Import menu consolidated to single shared static factory (mirrors export menu pattern).
- Label updated: "Save Metadata as Preset…" (was "Save as Preset…").

### Set Location Sheet

- Initial map zoom tightened from 0.12 → 0.02 (neighbourhood level).
- Coordinate block expanded to 2-column grid: lat/lng on row 1, selected advanced fields in subsequent rows as reverse geocode resolves them.
- Auto-reverse-geocode on sheet open when file has existing GPS coordinates; advanced fields populate immediately when ticked.
- `hasExplicitCoordinate` flag introduced to distinguish file-sourced coordinates from user-location map centering.
- User location fallback now only moves the map centre — session coordinates are never written from location services, preventing accidental GPS data being applied to files.

### Handoff / External Apps

- Photos / Lightroom / Lightroom Classic handoff passed.

## Known Parked Issues

None currently open.

## Remaining Smoke Worth Running

### Gallery Full Interaction Sweep

Re-run in one pass on the current candidate:
- rubber-band selection
- arrow-key movement
- zoom in / out
- Quick Look
- cmd-click
- shift-range selection

### Batch Rename Edge Cases

Run the following specifically on the current candidate (UI was overhauled — full re-test required):
- All six token types: Text, Current Name (all four components × all three casings), New Extension, Sequence Number, Sequence Letter, Date and Time
- Collision case
- No-op rename
- Extension override
- Restore after rename
- Inline preview updates correctly as tokens change
- Validation errors surface correctly (empty extension, empty name, duplicate)

### Date/Time + Location Workflow Sweep

Run one explicit pass on current candidate:
- Date/Time sheet modes: Shift / Time Zone / Specific / File
- Preview enable/disable behavior with and without effective changes
- Apply-to target tags behavior (Original / Digitised / Modified)
- Set Location: file with existing GPS — sheet opens at correct location at tighter zoom, advanced fields auto-populate on open, Adjust activates without re-searching
- Set Location: file without GPS — map centres on user location silently, no pin, Adjust disabled until explicit search or pin drag
- Set Location: search result populates pin and advanced fields correctly
- Set Location: pin drag populates advanced fields correctly
- Set Location: advanced fields toggled on/off in popover update grid correctly

### Post-Build Artifact Sanity

Run after archive / notarize / DMG creation:
- install the app
- launch
- open a folder
- make one metadata edit
- run one batch rename
- run one restore

### Streamlining Smoke (Phases 1-3)

Run on the final RC commit and record outcomes:

- [ ] ExifTool prune validation:
  - [ ] Brotli tests payload absent in app bundle.
  - [ ] Metadata read/apply + import + CSV export still pass.
- [ ] Release strip validation:
  - [ ] Release binary size captured.
  - [ ] dSYM presence confirmed.
- [ ] Preview/cache validation (Phase 2):
  - [ ] Large-folder selection sweep in list/gallery with inspector visible.
  - [ ] No major preview stalls or persistent blank previews.
  - [ ] Problematic/non-image thumbnail fallback behaves correctly (icon/downsampled fallback; no full-res blowup symptoms).
- [ ] Batch rename latency validation (Phase 3):
  - [ ] Rapid typing in token controls remains responsive.
  - [ ] Output parity checks pass: collision, no-op, extension override, restore.

## Important Notes

- Broad `swift test` now passes cleanly on this branch in current verification (`182` executed / `1` skipped / `0` fail).
- Performance/payload streamlining work for pre-ship is tracked in `docs/v1.2-performance-streamlining-plan.md`.
- Phase 1 streamlining status as of 2026-04-03:
  - ExifTool bundling now prunes Brotli test fixtures from app payload.
  - Release strip settings are enabled and verified in Xcode Release builds.
  - Observed size change in local Release build output: app bundle ~`36M` -> ~`30M`; app binary ~`10M` -> ~`4.2M`.
- Phase 2 streamlining implementation status as of 2026-04-03:
  - Inspector preview cache cap reduced (`600` -> `48`).
  - Preview warmup narrowed to selection-neighborhood candidates rather than whole-folder bulk preload.
  - Thumbnail fallback replaced with downsampled ImageIO decode path (no raw full-resolution fallback cache insertion).
  - Build verification passed (`swift build --target ExifEditMac`).
- Phase 3 streamlining implementation status as of 2026-04-04:
  - Batch rename planning/assessment now supports pre-sorted fast path to avoid duplicate sort work.
  - Batch rename date output/parsing now reuses cached formatters.
  - Batch rename live preview now uses lightweight debounce to reduce per-keystroke recompute churn.
  - Build verification passed (`swift build --target ExifEditMac`).
- SharedUI and Librarian also received Welcome-sheet parity commits during this conversation:
  - SharedUI: `a839c19` `feat: add shared SwiftUI welcome presentation`
  - Librarian: `d835fda` `refactor: present welcome as a SwiftUI sheet`
